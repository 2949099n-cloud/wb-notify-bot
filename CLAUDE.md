# wb-notify-bot

Мультитенантный Telegram-бот с уведомлениями о заказах Wildberries (новый заказ / отмена / выкуп) через официальный API WB (dev.wildberries.ru), плюс закреплённая ежедневная сводка и меню с доп. функциями. Один бот — много независимых пользователей, каждый подключает свой магазин сам через `/addshop`.

## Правила проекта (постоянные, не забывать между сессиями)

1. **Мультитенантность обязательна.** Магазины хранятся в БД (`shops`: `owner_user_id`, `telegram_chat_id`, зашифрованный токен, `token_status`), а не в `.env`. Каждая факт-таблица несёт `shop_id`. **Изоляция по `shop_id`/`owner_user_id` без исключений** — ни один запрос не должен возвращать данные чужого магазина.
2. **`shops.id` — `INTEGER PRIMARY KEY AUTOINCREMENT`**, никогда не переиспользуется (гарантия SQLite, не ручная дисциплина).
3. **WB-токен магазина хранится только зашифрованным** (`cryptography.Fernet`, ключ — `TOKEN_ENCRYPTION_KEY` в `.env` сервера, см. `security/tokens.py`). Расшифровывается только на момент конкретного вызова WB API (`shops_repo.get_decrypted_token`), не хранится в памяти дольше необходимого и никогда не логируется в открытом виде.
4. **Онбординг — `shops_repo.register_shop()`, общая функция для CLI и `/addshop`.** При добавлении магазина: живая проверка токена (`ping`), попытка авто-определить имя через `seller-info`, шифрование, запись. При `/addshop` в Telegram — **сообщение с сырым токеном удалять из чата сразу после обработки**.
5. **401 от WB — статус, не падение.** Помечать магазин `token_status='invalid'` (`shops_repo.mark_token_invalid`), писать алерт в `shop_alerts`, не ронять опрос остальных магазинов. Опрос всех активных магазинов — `asyncio.gather(..., return_exceptions=True)`.
6. **Лимит склада «1 запрос/мин» — per-token (per-магазин), не глобальный.** Не нужно разносить запросы МЕЖДУ разными магазинами, только повторные вызовы одного и того же (и так решается интервалом опроса 30 мин).
7. **Перед реализацией/правкой любого модуля в `wb_api/*`** — сначала WebSearch актуальной документации на dev.wildberries.ru (эндпоинты, поля, лимиты периодически меняются, не полагаться только на память).
8. **Тексты уведомлений (RU + эмодзи) в `telegram/formatters.py` менять нельзя** без явного запроса пользователя — формат утверждён и должен совпадать с референсным ботом FenixWB дословно.
9. **"Остатки подробно"** считается для трёх окон сразу — 7/30/60 дней (не одно дефолтное), по каждому складу отдельно (не агрегируя, в отличие от блока аналитики в обычном уведомлении).
10. **Денежная/расчётная логика (`calc/metrics.py`) обязательно с unit-тестами** — деление на 0 → `"∞"`, а не ошибка.
11. **WB API не поддерживает вебхуки** — только поллинг (15–30 мин), архитектура на этом не должна меняться.
12. Разработка — по шагам из плана ниже, с проверкой каждого шага, не писать всё сразу.

## Стек

Python 3.11+, `httpx.AsyncClient` (WB-клиент асинхронный — нужен для `asyncio.gather` по магазинам), `python-telegram-bot`, SQLite (`src/wbnotify/db.py`, доступ синхронный через `sqlite3`), `cryptography.Fernet`, `APScheduler`, `pytest`.

## Структура

```
src/wbnotify/
  config.py             # .env -> Config (Telegram-токен, ключ шифрования, интервалы; БЕЗ магазинов)
  db.py                  # схема SQLite (shops с owner_user_id, shop_alerts, ...)
  models.py              # ShopRow и т.д. — БЕЗ сырого токена в датаклассе
  security/tokens.py     # encrypt_token/decrypt_token (Fernet)
  shops_repo.py           # register_shop/list_active_shops/get_decrypted_token/mark_token_invalid —
                           # единая точка входа для CLI (шаг 2) и /addshop (шаг 4)
  wb_api/                 # асинхронные клиенты WB API (client.py: WBAuthError на 401, retry/backoff на 429/5xx)
  sync/                   # инкрементальный забор данных в БД, по магазину, вызывается через asyncio.gather
  events/classify.py      # детект Заказ/Отмена/Выкуп/Возврат, дедуп, notification_queue
  calc/metrics.py         # расчёты: % выкупа, скорость заказов, дней остатка
  telegram/                # formatters (шаблоны), sender, bot (/addshop и др.), keyboards, daily_summary
  scheduler/               # jobs, runner (точка входа процесса)
scripts/
  check_connectivity.py   # смоук-тест Telegram + ключа шифрования (шаг 1; WB-токены больше не здесь)
  sync_cli.py              # register (= /addshop без Telegram) + sync (шаг 2)
```

## Статус / шаги

- [x] Шаг 1 — заготовка проекта, схема БД, `scripts/check_connectivity.py` (Telegram getMe/sendMessage подтверждены вживую)
- [x] Ревизия 2 — пивот на мультитенантность: схема `shops`/`shop_alerts` (+`subscription_status`/`subscription_expires_at`, гейт `shops_repo.is_shop_active()`), шифрование токенов, `shops_repo.register_shop` подтверждён вживую реальным WB-токеном
- [x] Шаг 2 — `wb_api/{orders,sales,stocks,content}.py` + `sync/{orders,sales,stocks,cards}_sync.py`, подключено к `sync_cli.py sync`. Подтверждено вживую на реальном магазине: 14058 orders / 5512 sales / 181 stocks / 176 cards; повторный `sync --all --once` даёт 0 новых строк (идемпотентность подтверждена); 401→invalid+alert, изоляция от других магазинов покрыта тестом `tests/test_sync_auth_error.py`. **`wb_api/{analytics,tariffs}.py` (рейтинг/отзывы, комиссия/логистика) — НЕ реализованы**, нужны для блока аналитики в уведомлениях (шаг 3/4), это осознанно отложено, не блокирует шаг 2.
- [x] Шаг 3 — `events/classify.py` (Заказ/Отмена/Выкуп/Возврат, дедуп через notified_*_at) + `counters.py` ([#N] по МСК-суткам) + `calc/metrics.py` (rate/velocity/stock_eta + БД-обёртки). 19 unit-тестов зелёные (`tests/test_classify.py`, `tests/test_calc_metrics.py`). Подтверждено на реальных данных: 14058 orders → 8664 cancel + 5218 buyout + 294 return событий в notification_queue за 0.88с; повторный проход — 0 новых (дедуп на масштабе подтверждён). **Найден реальный нюанс**: "Склад WB" (warehouseId=-999999) из stocks-report — похоже, виртуальный агрегат, не совпадает по имени с реальными `orders.warehouse_name` → `stock_days_by_warehouse` для него всегда даёт "∞" (корректно, не крашится, но фактически бесполезно для этого случая) — разобраться на шаге 6 при реализации «Остатки подробно».
- [x] Шаг 4 — `telegram/formatters.py` (точные шаблоны Заказ/Отмена/Выкуп + блок аналитики), `telegram/sender.py` (карусель фото media group + caption, fallback на >1024 символов с ровно той пометкой из ТЗ), `telegram/bot.py` (`/start`, `/addshop` через ConversationHandler + `/cancel`, ровно `shops_repo.register_shop`). 28 тестов зелёных. **Подтверждено вживую на реальных, свежих (не архивных) данных.** `/addshop` проверен юнит-тестом с моками (delete_message вызывается ВСЕГДА — и при успехе, и при невалидном токене), не живым интерактивным вводом в Telegram (агент не может печатать в чужом Telegram).
  - **Баг (найден пользователем при живой проверке, исправлен)**: поле "🆔" показывало `chrtID` (внутренний системный id размера WB) вместо `supplierArticle`/`vendorCode` (артикул продавца) — визуально похоже на случайный номер вместо осмысленного артикула. Добавлена колонка `orders.supplier_article`/`sales.supplier_article` (миграция `_migrate()` в `db.py` — ALTER TABLE ADD COLUMN с проверкой существования, идемпотентно), заполняется из `raw_json.supplierArticle` при синке; существующие строки задним числом бэкфилены.
  - **Пробел против ТЗ (найден пользователем, закрыт)**: ТЗ требует "Фото (карусель)", было — одно фото. Добавлено `cards_cache.photos_json` (первые 3 фото карточки), `sender.py` шлёт `send_media_group`.
  - **Реальный баг Telegram API (найден при живой отправке карусели, исправлен)**: `sendMediaGroup` с фото по URL иногда падает с `"Failed to get http url content"` на КОНКРЕТНОМ фото из тройки, хотя та же ссылка отдаёт 200 OK при прямом curl — воспроизводится стабильно (3 попытки подряд), не транзиентно. Похоже на баг/лимит именно в серверном URL-фетчере Telegram, не в WB CDN. Исправлено кардинально: `sender.py` теперь качает байты фото сам (`httpx`, retry) и грузит в Telegram как файл, а не по URL; если конкретное фото не скачалось — пропускается, карусель уходит без него; если не скачалось ни одного — текстовый фоллбек. Покрыто тестами (`test_one_broken_photo_in_carousel_is_skipped_not_fatal`, `test_all_photos_broken_falls_back_to_text_only`).
  - **Уточнение от пользователя**: скриншот с "Fenix WB | Bot" в предыдущем обсуждении — это референсный бот для сравнения, не наш `@OderWB_bot`; полные данные там (комиссия, ИЛ, рейтинг) не означают, что наш `/orders`/`/sales` их отдаёт — не отдаёт, см. ground truth в разделе выше.
  - **Открытый вопрос, не решён**: `orders`/`sales` в БД содержат данные за март–август 2026 (6 месяцев), хотя WB документирует хранение 90 дней. Не мешает работе (данные реальные, синкаются корректно), но стоит перепроверить на другом магазине/токене — возможно, особенность конкретного (тестового?) аккаунта, а не общее поведение API.
  - «Возврат» использует тот же шаблон, что «Выкуп» (в ТЗ помечен опциональным, "по аналогии").
- [x] `wb_api/tariffs.py` + `wb_api/analytics.py` + `sync/{tariffs,analytics}_sync.py` + `calc/tariffs.py`. Проверено вживую: 7410 категорий комиссии, 12 складов логистики, рейтинг/отзывы для 166/176 карточек.
  - **Комиссия WB — теперь настоящая.** `GET /api/v1/tariffs/commission` (common-api), поле `subjectName` совпадает 1:1 с `orders.subject`/`sales.subject` (проверено на реальных категориях). `kgvpSupplier` для FBW (`warehouseType == "Склад WB"`), `kgvpMarketplace` для FBS/DBS — см. `calc/tariffs.is_fbw()`.
  - **Оценка/Отзывы — теперь настоящие.** `POST /api/analytics/v2/item-rating` (seller-analytics-api). Найден и исправлен реальный баг при первом же прогоне: WB отвечает 400 "current period end date cannot be today", если `currentPeriod.end == сегодня` — исправлено на `end = вчера`. **Отзывы — не лайфтайм-счётчик**: `feedbackCount.current` зависит от periода запроса (0 отзывов за месяц, 2 за 365 дней у одного и того же товара — проверено вживую), максимально широкий разрешённый период — 365 дней (шире — 400). Это ограничение самой API, не наше приближение. `feedbackRating.current` (рейтинг), наоборот, от периода не зависит.
  - **Логистика WB/Тариф/ИЛ — заполняются, когда есть совпадение по складу, иначе честно "—".** `GET /api/v1/tariffs/box` (common-api) возвращает `warehouseList[]`, но проверено вживую: список складов там — крупные логистические хабы/страны (Атакент, Ташкент, "Свой склад РФ" и т.п.), а НЕ конкретные названия фулфилмент-центров из `/orders` ("Коледино", "Воронеж", "Склад WB РФ") — реального совпадения по имени для этого магазина не нашлось ни разу. Похоже, единственный эндпоинт с коэффициентами по конкретным складам — `/api/tariffs/v1/acceptance/coefficients` — **временно отключён самим WB** ("Метод временно отключён", проверено в доках). Это ограничение WB API на сегодня, не что-то, что можно доделать правкой кода. Формула суммы логистики (`_estimate_logistics_sum` в `formatters.py`) — общепринятая оценка (база + доп.литры×ставка, ×ИЛ-коэффициент), WB официально формулу не публикует.
  - `client.py` заодно доработан: 4xx (кроме 401/429) теперь оборачиваются в `WBError` с телом ответа вместо голого `httpx.HTTPStatusError` — было видно как "неожиданная ошибка синка" с трейсбеком вместо понятного сообщения при первом прогоне analytics.
- [x] Ревизия по репорту пользователя с реальных уведомлений (три реальных бага, найдены и исправлены на живых данных):
  - **% выкупа мог давать >100% (реально видели 200%) — исправлено.** Было: `buyout_rate_with_returns` считала заказы/выкупы/возвраты ТРЕМЯ независимыми `COUNT` по трём независимым датам — числитель и знаменатель были буквально из разных выборок (выкуп чужого, более раннего заказа засчитывался в текущее окно). Стало: когортный расчёт — берём `srid` заказов ИЗ окна, считаем выкупы/возвраты ТОЛЬКО по этим `srid` (сама продажа может случиться позже окна — это нормально, доставка не мгновенная). Проверено на всех реальных данных: 714 комбинаций (nm_id, размер, окно) — 0 случаев >100% (было минимум 3 подтверждённых случая 200%). Regression-тесты: `test_buyout_rate_never_exceeds_100_percent_from_unrelated_cohorts`, `test_buyout_rate_counts_buyout_even_if_sale_happens_after_window`.
  - **Все оконные метрики ("Вчера/Сегодня", % выкупа, скорость заказов, продаж за N дней) считались от РЕАЛЬНОГО текущего момента, а не от даты события в уведомлении — исправлено.** `_card_analytics_block` в `formatters.py` теперь принимает `asof` (дата события: `order.date`/`cancel_date`/`sale.date`) и прокидывает его во все расчётные функции. Раньше для старых/бэкфилленных уведомлений (например, заказ за март) блок аналитики всё равно считался "на сегодня" — отсюда одинаковое "Вчера всего: 12 на 45929₽" в разных уведомлениях с разными датами. Побочно найден и исправлен второй баг того же класса: оконные запросы (`order_velocity`, `sales_count`) не имели ВЕРХНЕЙ границы окна — для исторического `asof` окно "утекало" в реальное будущее. `stock_days_aggregate`/`stock_days_by_warehouse` **сознательно** остались без `asof` (используют текущий момент) — `stocks_current` это снимок остатков БЕЗ истории по датам, делить его на историческую скорость было бы менее осмысленно, чем на текущую.
  - **Карусель фото — теперь квадратная, не вертикальные карточки.** WB отдаёт товарные фото 900x1200 (3:4, проверено вживую скачиванием реального файла) — в Telegram media group это рендерилось высокими вертикальными карточками. `sender.py` теперь кадрирует каждое фото по центру в квадрат (Pillow) перед отправкой.
  - **FBO/FBS — уже распознаётся правильно, отдельного бага не было.** Разграничивающее поле — `orders.warehouse_type`/`sales.warehouse_type` (WB: `warehouseType`, `"Склад продавца"` = FBS, `"Склад WB"` = FBO/FBW) — уже использовалось в `calc/tariffs.is_fbw()` для выбора `kgvpMarketplace`/`kgvpSupplier`. Подтверждено на реальных заказах: 215 FBS / 13846 FBO в БД, комиссия корректно различается (42% FBS vs 45% FBO для «Ботинки»). Логистика не находится ни для FBS, ни для FBO — по разным причинам (см. пункт выше про `/tariffs/box`).
  - **Обратная логистика WB (при отмене) — эндпоинт найден, но НЕ подключён, ждёт решения пользователя.** `GET /api/v1/tariffs/return` (common-api, `?date=`) — в отличие от `/tariffs/box`, его `warehouseList[]` РЕАЛЬНО содержит `"Склад WB РФ"` (точное совпадение с `orders.warehouse_name` для части заказов этого магазина). НО: поля не задокументированы вообще (доки дают пустой `{}` в примере ответа) — `deliveryDumpKgt/Srg/Sup + Office/Courier + Base/Liter/ReturnExpr`, разбитые по категориям товара (Kgt/Srg/Sup — вероятно категории по габаритам). Для «Склад WB РФ» реально пришли: `deliveryDumpSupReturnExpr="250"`, `deliveryDumpKgtReturnExpr="1050"`, `deliveryDumpSrgReturnExpr="255"`, Base/Liter — "не принимает". Не подключено в код — какое именно поле показывать как "Обратная логистика WB", нужно решить пользователю (это денежное поле).
  - **СПП — бага не нашли.** `orders.spp`/`sales.spp` берутся напрямую из поля `spp` в ответе WB (`row.get("spp")`), нигде не пересчитываются вручную. Если расхождение с реальным кабинетом WB всё же есть — нужен конкретный пример (артикул + дата), чтобы сверить.
  - **"Реализованная" комиссия по конкретному заказу — НЕ найдена в `/orders`/`/sales`.** Оба эндпоинта дают только цены (`totalPrice`/`priceWithDisc`/`finishedPrice`/`spp`) и `forPay` (для sales) — ни явного поля комиссии, ни поля вроде `paymentSaleAmount` с понятной документированной семантикой. Похоже, фактическая ("реализованная") комиссия по конкретной продаже доступна только в периодических финансовых отчётах о реализации (`/docs/openapi/financial-reports-and-accounting`), которые публикуются с задержкой в дни/недели — НЕ в момент события. Если это подтвердится, текущий подход (справочная ставка `/tariffs/commission` по категории+схеме, уже с поправкой на FBO/FBS) — это лучшее доступное приближение в реальном времени, не то, что можно легко заменить на "точную" цифру.
- [x] Ревизия 2 по репорту пользователя (сверка с реальным кабинетом WB Partners, скриншоты):
  - **Комиссия WB — было перепутано поле, исправлено.** `kgvpSupplier` НЕ равен комиссии FBW, несмотря на название — пользователь сверил с реальным кабинетом (Тарифы → Комиссия, категория «Ботинки»): «Склад WB (FBW)» = 37.5% в кабинете = `paidStorageKgvp` в API (не `kgvpSupplier`, который там оказался 45% — это графа «Витрина(DBS)/Курьер WB(DBW)», не имеющая отношения к нашей модели). «Маркетплейс (FBS)» = 42% = `kgvpMarketplace` — эта половина была верна изначально. Исправлено в `calc/tariffs.get_commission_pct`. Регрессионных тестов на конкретные % пока нет (значения зависят от живого API) — таблица `tariffs_cache` перепроверяется вручную по кабинету при следующих правках.
  - **"Вчера всего" — занижено было из-за протухших данных (синк ~11ч назад, шага 5 ещё нет), но помимо этого источник данных был концептуально не тот.** Решение пользователя (реализовано): источник "Вчера/Сегодня" зависит от ТИПА уведомления — в «Заказ» считаем по `orders`, в «Выкуп» — по `sales`(is_return=0), в «Отмена заказа»/«Возврат» — по `sales`(is_return=1, т.е. по возвратам). Добавлен параметр `event_type` в `yesterday_today_breakdown` (`calc/metrics.py`, таблица `_BASIS_BY_EVENT_TYPE`) и прокинут через `_card_analytics_block`/`format_*_message`/`sender._render`. Проверено на реальных данных: «Заказ»-уведомление теперь даёт «Вчера всего: 31 на 258195₽» — точное совпадение с кабинетом пользователя (было 12/45929₽ по старой логике). Тест: `test_yesterday_today_basis_depends_on_event_type`.
  - **Карусель — квадратный кроп не помог, фото 1 всё равно рендерилось Telegram'ом отдельно (подтверждено даже без подписи вообще, диагностический тест).** Решение пользователя: не кадрировать в квадрат, а уменьшать разрешение, СОХРАНЯЯ исходный аспект 3:4 (`sender._resize_for_carousel`, `PHOTO_MAX_DIMENSION=480`). Функция переименована из `_center_crop_to_square`. **Не проверено вживую, ждём скриншот от пользователя** — есть подозрение, что раскладка альбома (1+2 вместо 3-в-ряд) может быть особенностью рендера Telegram-клиента, не зависящей ни от аспекта, ни от разрешения, ни от подписи — если новый вариант тоже не поможет, это подтвердит гипотезу и вопрос закроется как ограничение Telegram, не наш баг.
  - **Найден и исправлен реальный таймаут при отправке.** `sendMediaGroup` с 3 фото стабильно (не разово) падал с "Timed out" на дефолтных таймаутах python-telegram-bot (read/write=5с) — увеличено до 30с (media_write_timeout=60с) через `HTTPXRequest`, вынесено в `telegram/sender.make_bot()`, используется и в `notify_cli.py`, и в `telegram/bot.py`.
  - **Побочно найден и исправлен баг в analytics.py**: `start = end - 365` (где `end` = вчера) иногда даёт 400 "start earlier than a year ago" — оказалось, WB считает лимit 365 дней от СЕГОДНЯ, а не от `end`. Исправлено: `start = сегодня - 365`.
  - **Логистика WB (`/tariffs/box`) — добавлен явный фоллбек на эталонный склад, решение пользователя.** Для складов без точного совпадения (подавляющее большинство реальных заказов — Коледино, Тула и т.п., их в `/tariffs/box` просто нет) теперь всегда используются Marketplace-поля склада **"Свой склад РФ"** (`base=78,2 liter=23,8 coef=170%`) — независимо от FBW/FBS исходного заказа, явный выбор пользователя, не выбор по схеме. Реализовано в `calc/tariffs.get_box_tariff` (`FALLBACK_WAREHOUSE_NAME`). Проверено вживую: «Коледино» и «Склад WB РФ» (оба без совпадения) корректно отдают эталон; «Атакент» (есть совпадение) — отдаёт свои настоящие цифры, не эталон.
  - **Обратная логистика — РЕАЛИЗОВАНА по официальной методике WB (закрыто).** Пользователь прислала справку ЛК «Виды логистики и расчёт стоимости»: обратная доставка считается ТОЛЬКО от объёма товара, коэффициент склада и ИЛ в ней **не участвуют** — поэтому per-warehouse поля `/api/v1/tariffs/return` («не принимает») к этому расчёту отношения не имели, а «25₽+4₽/л» одинаковое во всех строках кабинета — это другая графа, не наша. Реализовано в `calc/tariffs.get_return_logistics_sum`: до 1 л — таблица диапазонов (23/26/29/30/32₽), от 1 л — `46₽ + 14₽ × (объём − 1)`. Тесты `tests/test_calc_tariffs.py` сверены ДОСЛОВНО с примерами WB (1,8 л → 57,2₽; 65 л → 942₽). **Кросс-валидация**: для товара из референсного скриншота FenixWB (nmId 342705822, 4,68 л) наша формула даёт 97.52₽ — цифра в цифру совпадает с референсным ботом.
  - **ИЛ (индекс локализации) — временно отменён WB, показываем 0.** Раньше в строку «ИЛ» ошибочно подставлялся `boxDeliveryCoefExpr` (170%) — это КОЭФФИЦИЕНТ ДОСТАВКИ СКЛАДА, другая величина; он корректно продолжает участвовать в расчёте суммы логистики (сумма подтверждена пользователем), но как «ИЛ» его показывать нельзя. Строка и логика намеренно НЕ удалены (`_IL_DISABLED_VALUE` в `formatters.py`) — если WB вернёт ИЛ, менять нужно будет только источник значения.
  - **Старая формулировка про обратную логистику (устарела, оставлена для истории):** Пользователь попросила брать поле "Доставка на ПВЗ (базовая цена за 1 л + доп. литр)" со страницы кабинета `/dynamic-product-categories/return-cost` (25₽+4₽/л для «Склад WB РФ»). Живой ответ `/api/v1/tariffs/return` для «Склад WB РФ» на это же поле (Sup/Kgt Office Base/Liter) отдаёт `"не принимает"` — прямое противоречие. Дополнительно: в скриншоте кабинета "25₽+4₽/л" ОДИНАКОВОЕ для всех складов без исключения — похоже на плоскую платформенную константу, а не поле конкретного склада. Не стали гадать/хардкодить — нужно уточнение, откуда в API брать это значение.
  - **Скидка/штраф FBS за скорость отгрузки — найдена официальная формула (новость WB Partners от 16 июля, скрины пользователя), но не реализуема при текущем наборе данных.** С 31 августа: штраф (не скидка) за отгрузку позже 18ч считается по `Рц × (Тф − Тн) × Ко` для FBS/DBW, где `Тф`/`Тн` требуют момента ПЕРЕВОДА ЗАКАЗА В СТАТУС «В доставке» — не заказа, не выкупа. Скидка за отгрузку ≤13ч (−1.5 п.п. к комиссии) этим обновлением не затронута, по-прежнему просто встроена в итоговую комиссию. **Ни `/api/v1/supplier/orders`, ни `/sales` не содержат момент передачи в доставку/статус сборочного задания** — это поле есть только в других API (Marketplace/FBS orders API, статусы сборочных заданий), пока не интегрированных в проект. Нужен отдельный интеграционный модуль, не точечная правка — не начато.
  - Присланный файл `Новости.html` в начале этого раунда оказался пустой HTML-оболочкой SPA (без данных) — пользователь переслала содержимое скриншотами, см. пункт про скидку/штраф FBS выше.
- [x] **Модуль FBS (Marketplace API) — скидка/штраф за скорость отгрузки.** Реализован по подтверждённому плану, проверен вживую.
  - **Права:** текущий токен магазина УЖЕ имеет доступ к `marketplace-api.wildberries.ru` — пересоздавать не нужно (`/ping` → 200, `/api/v3/orders/new` → реальные задания).
  - **Тф (момент передачи в доставку) — найден.** В `/api/v3/orders/status` метки времени НЕТ, только текущий статус (`supplierStatus`: new/confirm/**complete**=«В доставке»/cancel). Но у поставки `/api/v3/supplies/{id}` есть **`closedAt`** — момент закрытия поставки = момент передачи в доставку. Это даёт Тф **ретроспективно**, поллинг для отлова перехода не нужен.
  - **Связка с Statistics API:** `fbs_assembly_tasks.rid` == `orders.srid` (проверено: совпало 85 из 100; остальные — вне 90-дневного окна). Оба времени Marketplace API в UTC, `orders.date` — МСК (+3ч), совпадают секунда в секунду; смешивать их в одном вычитании нельзя, поэтому часы считаются по двум полям Marketplace API.
  - **Лимиты:** 300 запросов/мин (всплеск 20) — на два порядка мягче Statistics API, отдельный цикл поллинга не нужен, встроено в общий `sync_shop_all`.
  - **Схема:** `fbs_assembly_tasks` (задания) + `fbs_supplies` (кэш поставок — у одной поставки десятки заданий: в живом тесте 136 заданий = 28 поставок, дёргать `/supplies/{id}` на каждое задание бессмысленно). Закрытые поставки повторно не запрашиваются.
  - **Пороги (ЛК → Меры поддержки, действуют до 31.01.2027)** в `calc/fbs_shipping.py`: до 13ч −5 п.п.; 13–42ч −3,5 п.п.; 42–48ч базовая; 48–54ч +0,30 п.п./ч сверх 48-го; 54–60ч +0,35; свыше 60ч +0,45. С 31.08.2026 надбавка берётся **отдельным штрафом в рублях** (`Рц × То × Ко`), а не повышенной комиссией — поэтому в уведомлении это две разные строки. 18 тестов, включая пример WB (просрочка 2ч × 0,3 п.п. → 500₽ × 0,6% = 3₽).
  - **Подтверждено вживую:** FBS-заказ «Ботинки», отгружен за 8.35ч → 42% базовая − 5 п.п. = **37%** в уведомлении. Строка «⚠️ Штраф за отгрузку» показывается только при реальной просрочке (>48ч).
  - **Баг, пойманный при первом прогоне:** WB отдаёт `supplyId` пустой строкой вместо отсутствия поставки → URL `/supplies/` собирался без id и упирался в 301. Нормализуем в NULL при записи + фильтр `!= ''` в выборке.
  - **Корректировка комиссии применяется только к «Выкуп»/«Возврат», не к «Заказ»** (решение пользователя): в момент заказа товар физически не отгружен, поставка не закрыта — скидка/штраф ещё не определены. Проверено на реальных FBS-выкупах: nm 210443841 (отгрузка 21.75ч → −3,5 п.п.: 46%→**42.5%**), nm 492626896 (отгрузка 11.81ч → −5 п.п.: 42%→**37%**); в уведомлении «Заказ» по тем же заказам — базовые 46% и 42%.
  - **«🚚 FBS склад продавца»** вместо названия склада для FBS-заказов: WB отдаёт в `warehouse_name` «Склад WB РФ» даже для FBS (проверено), подставлять это как склад отправки некорректно.
  - **«👑 <бренд> (<продавец>)»** — бренд берётся из карточки товара, а не из магазина: у продавца их несколько (реально в БД: `NILONIL` 88 карточек, `0NILONIL` 73, `0NIL0` 14), и он разный от товара к товару. В скобках — название продавца.
  - **Сумма в «Вчера/Сегодня» — `priceWithDisc`, а не `finishedPrice` (исправлено).** Сверено с аналитикой WB пользователя: выкупы за 01.09 = 8 шт / 67248 ₽, наш `price_with_disc` дал ровно 67248 ₽ (совпадение до рубля), `finished_price` давал 39459 ₽ — мимо. Количество выкупов совпало точно (8 и 13).
  - **ЗАКАЗЫ «вчера/сегодня» — из ВОРОНКИ ПРОДАЖ, а не из `/orders` (закрыто).** Пользователь сверяется со «сводкой на главной» в ЛК. Statistics API `/orders` (`flag=1`) на 02.09 и 01.09 отдаёт ровно 30 и 30 заказов, а сводка показывает 37 и 38. Прямой запрос `POST /api/analytics/v3/sales-funnel/products` за те же дни дал **37 шт / 266340 ₽** и **38 шт / 281348 ₽** — совпадение со сводкой до рубля. Значит, сводка питается воронкой, и для блока «Вчера/Сегодня» по заказам берём её (`sales_funnel_daily`, `sync/funnel_sync.py`, синкаются только сегодня+вчера). **Гранулярность воронки — nmId, размеров в ней нет**, поэтому «Вчера/Сегодня ТАКИХ» в уведомлении «Заказ» = по артикулу, а не по артикулу+размеру. Для исторических уведомлений (воронка синкается только за 2 дня) — фоллбек на `orders`, чтобы не показывать нули.
  - **Выкупы «вчера/сегодня» остаются на наших `sales`** — воронка для них НЕ подходит: она относит выкуп к дню, когда товар был ЗАКАЗАН (за 02.09 воронка дала 0 выкупов при реальных 13). Наши `sales` совпали со сводкой точно: 8 шт / 67248 ₽ за 01.09.
  - **«К выплате» = `forPay` из API как есть, штраф НЕ вычитается (решение пользователя).** Штраф за долгую отгрузку показывается отдельной строкой «⚠️ Штраф за отгрузку» — WB с 31.08.2026 и сам списывает его отдельным удержанием, а не внутри комиссии, поэтому смешивать их в одной цифре не нужно.
  - **Состав блока аналитики зависит от события (решение пользователя):** в «Возврате» не показываются «Оценка», «Отзывы» и «Выкуп/с учётом возврата»; в «Продаже» скрыт только «Выкуп/с учётом возврата». Заголовок «Выкуп» переименован в **«Продажа»**, поле «Цена выкупа» → «Цена продажи».
  - **Сумма остаётся `priceWithDisc`, расхождение со сводкой объяснено (решение пользователя — не подгонять).** Разница 14.88 ₽ на 13 выкупах возникла из ОДНОЙ продажи: фактическая скидка 59.941 %, но WB в поле `discountPercent` отдаёт округлённые 60. Сводка считает `totalPrice × (1 − discountPercent/100)` = 25100 × 0.4 = 10040 ₽, а `priceWithDisc` = 10054.88 ₽ (настоящая сумма сделки). Сумма всех 13 по формуле сводки = 89402.60 ≈ 89403 ₽ (её цифра), по `priceWithDisc` = 89417.88 ₽. Осознанно оставляем точное значение, а не огрублённое под сводку.
  - **«Возврат» — собственный шаблон** (`format_return_message`), больше не переиспользует «Выкуп»: возврат и отмена заказа — разные события. Поля по образцу референсного бота: `🔄 Возврат [#N]`, «Дата продажи», «N суток с даты ПРОДАЖИ» (не с заказа), «Дата возврата», «К возврату», «К выплате», «Обратная логистика WB». Дата продажи ищется по тому же `srid` среди `sales` с `is_return=0`.
  - **Знак сумм в «Вчера/Сегодня» различается по типу события** (решение пользователя): источник у «Отмены» и «Возврата» один (возвраты, WB хранит суммы отрицательными), но в «Возврате» минус сохраняем, в «Отмене заказа» показываем просто сумму (`abs`).
  - **Сообщения уходят с `parse_mode=HTML`** — понадобился жирный `<b>FBS</b>` в строке склада. ВАЖНО: любой подставляемый из данных текст (название товара, бренд, артикул, склад, регион) обязан идти через `_esc()` — в реальных данных встречаются `& < >`, без экранирования Telegram отвергнет сообщение целиком.
  - **Формат остатков и база расчёта дней (решение пользователя).** Строка: `38 (2шт+1шт) ≈ на 10 дн. (по заказам)` — первое слагаемое остаток на складах WB, второе на своих складах продавца, оценка «на N дн.» считается от их СУММЫ. База зависит от типа уведомления: «Заказ»/«Отмена» — по заказам, «Выкуп»/«Возврат» — по продажам (`stock_days_aggregate(basis=...)`, `_STOCK_BASIS_BY_EVENT_TYPE`). Тест: `test_stock_days_basis_orders_vs_sales`.
  - **Остатки — два источника.** Свои склады продавца (FBS) синкаются из Marketplace API (`/api/v3/warehouses` + `/api/v3/stocks/{id}`, связка по sku↔`cards_cache.sizes_json`) в ту же `stocks_current`, но с `warehouse_kind='seller'`; каждый синк заменяет снимок только своего вида складов. В блоке «Остаток» показываются оба: `38 (WB: 2 шт. + свой: 1 шт.)`. Проверено вживую: 2 склада продавца («ФФ Москва», «ФФ ФБС Спб»), 52 позиции с остатком.
  - **Фото:** от карусели отказались (решение пользователя). Telegram упорно рендерил первое фото альбома отдельным крупным блоком независимо от аспекта (3:4 и 1:1), разрешения и наличия подписи; уменьшение до 160px дало размытие без изменения раскладки. Шлём одно главное фото в оригинальном качестве. `photos_json` (3 фото) в БД сохранён, `Pillow` удалён из зависимостей за ненадобностью.
- [x] Шаг 5 — планировщик (`scheduler/jobs.py`, `scheduler/runner.py`, точка входа `scripts/run_scheduler.py`). Проверен вживую.
  - **Два расписания:** `poll_and_notify` каждые `POLL_INTERVAL_MINUTES` (синк частых источников → `classify_shop_events` → рассылка) и `daily_refresh` в `CARDS_REFRESH_HOUR_MSK` (карточки, рейтинги, тарифы). Оба с `coalesce=True, max_instances=1` — прогоны не наслаиваются, если предыдущий затянулся на 429-ожиданиях.
  - **Синк разделён на частый и суточный** (`FREQUENT_STEPS` / `DAILY_STEPS` в `sync/__init__.py`): карточки (2 запроса на 176 позиций) и рейтинги (4 батча, регулярно ловящие 429 с ожиданием ~15с) меняются редко и не стоят прогона каждые полчаса. `sync_shop_all` остался для ручного `sync_cli`.
  - **ГЛАВНОЕ — защита от рассылки бэкфилла.** На момент включения планировщика в очереди лежало **28 279** событий первичного бэкфилла; без отсечки первый же цикл вывалил бы их все пользователю в чат. Введено поле `shops.notify_from`: рассылаются только события, попавшие в очередь ПОСЛЕ этой метки, `NULL` = не рассылать ничего (безопасное значение по умолчанию). Исторические строки не удаляются — остаются в очереди для разбора. Включение: `python scripts/notify_cli.py start-notifying --shop-id N`. Покрыто тестами (`test_backfill_is_not_sent_when_notify_from_unset`, `test_only_events_after_notify_from_are_sent`).
  - **Второй предохранитель:** `DRAIN_LIMIT_PER_CYCLE = 30` — даже если очередь по какой-то причине разрослась, за один цикл уходит не больше 30 сообщений.
  - **Подтверждено вживую:** стартовый синк подхватил 27 новых заказов; цикл `poll_and_notify` отработал полную цепочку (синк → 97 новых событий → отправлено 30, остальные в следующие циклы); в очереди корректно остались 28 279 исторических (не будут отправлены никогда) и свежие сверх отсечки.
  - **Интервал опроса — 5 минут** (решение пользователя: уведомления должны приходить постепенно, а не пачкой раз в полчаса). Лимиты WB это позволяют: `/orders` и `/sales` — 1 запрос/мин. Замер живого цикла: ~35 секунд, укладывается с большим запасом.
  - **Два независимых фильтра рассылки, каждый под свой сценарий (важно не путать):**
    1. `created_at >= shops.notify_from` — отсекает первичный бэкфилл (события, попавшие в очередь ДО включения рассылки). Опоздавшие события он пропускает, и это правильно.
    2. `event_date >= сегодня − MAX_EVENT_AGE_DAYS` (7 дней) — страховка от совсем древних событий, если они впервые классифицируются только сейчас. Колонка `notification_queue.event_date` (order.date / cancel_date / sale.date) заполняется в `classify.py`; по `created_at` такое событие выглядело бы свежим.
  - **Почему именно ОКНО, а не «не раньше даты включения» (замечание пользователя, ошибка исправлена).** WB может отдать заказ/продажу с опозданием на день-два-три — жёсткая отсечка по дате включения выбрасывала бы такие события навсегда. Окно в 7 дней пропускает опоздавших и продолжает отсекать архив. Тесты: `test_late_delivered_event_is_still_sent`, `test_ancient_event_classified_late_is_not_sent`.
  - **Догон после сбоя работает по конструкции:** очередь персистентна, разбирается по возрастанию `id`, поэтому после простоя сначала уходит непереданное, затем текущее; `DRAIN_LIMIT_PER_CYCLE` не даёт вывалить всё разом. Тест: `test_backlog_after_outage_is_sent_oldest_first`.
  - **Ограничение по хостингу:** пока процесс не запущен, бот не опрашивает WB и ничего не присылает — именно это было причиной «протухших» цифр при ручных проверках. На локальной машине работает только пока она включена.
- [x] Шаг 6 — меню под уведомлением (`telegram/keyboards.py`, `on_button`/`/settings`/`/unmute` в `telegram/bot.py`), 76 тестов зелёные. **На сервере ещё не развёрнут** (SSH недоступен, см. ниже).
  - Четыре кнопки: «Остатки подробно», «На WB» (URL на карточку), «Не показывать такие» (мьют типа события), «Внешний вид». `callback_data` укладывается в 64 байта (проверено тестом) — поэтому в него кладём только `shop_id`/`nm_id`/`event_type`/размер, остальное дочитываем из БД.
  - Мьют хранится в `chat_mutes` (chat_id + event_type + shop_id). Замьюченные события **не удаляются**, а просто не выбираются из очереди — остаются pending и уходят из выборки сами, когда выпадут из окна давности. Снять — `/settings` или `/unmute`.
  - «Остатки подробно»: по каждому размеру общий остаток и разбивка «Склады WB / свой склад». **Окна 7/30/60 дней убраны** (решение пользователя) — в детальном виде остаются только количества.
  - В самом уведомлении у строки остатка убрана пометка «(по заказам)»/«(по продажам)»; база расчёта по-прежнему зависит от типа события, просто не пишется в текст.
  - **Фото — снова три, уменьшенные вчетверо до 450x600** (решение пользователя; исходник WB 900x1200, аспект 3:4 сохраняется, `sender._resize_photo`). `Pillow` вернулся в зависимости.
  - «Настроить внешний вид» — заглушка, полный набор чекбоксов по полям уведомления не сделан: не решено, какие именно поля должны отключаться.
  - **Развёртывание шага 6 не выполнено: сервер 94.228.115.174 не пускает по SSH** — порт 22 дропает пакеты (таймаут), при этом 80/443 принимают TCP, то есть машина жива. Похоже на файрвол Timeweb или `fail2ban`. Обновление делается однострочником из VNC-консоли панели: `cd /opt/wb-notify-bot && git pull && bash deploy/update.sh` (см. `deploy/README.md`). На сервере сейчас работает предыдущая рабочая версия — состояние безопасное.
- [ ] Шаг 7 — доп. тесты расчётных функций (edge-кейсы)

## Проверенные вживую факты о WB API (важно, расходятся с исходным ТЗ — не доверять памяти/ТЗ, доверять этому списку)

- `/api/v1/supplier/orders` и `/api/v1/supplier/sales` (statistics-api.wildberries.ru) — **НЕ возвращают `chrtId`**, только `nmId`+`techSize`+`barcode`. Лимит: orders 1 запрос/мин (всплеск 10), sales 1 запрос/мин (без всплеска, легко словить 429 при ручном тестировании).
- Остатки по складам — старый `/api/v1/supplier/stocks` отключается WB. Актуальный эндпоинт: `POST https://seller-analytics-api.wildberries.ru/api/analytics/v1/stocks-report/wb-warehouses`, body `{nmIds, chrtIds, limit, offset}`, ответ `data.items[]` с полями `nmId, chrtId, warehouseId, warehouseName, regionName, quantity, inWayToClient, inWayFromClient` — **НЕТ techSize/barcode**. Токен категории "Аналитика". Лимит 3 запроса/20с, всплеск 1.
- Связка chrtId↔techSize↔barcode — только через `POST /content/v2/get/cards/list` (content-api.wildberries.ru), поле `sizes[]` каждой карточки: `{chrtID, techSize, wbSize, skus[]}`. Оттуда же `dimensions.{length,width,height,weightBrutto}` для габаритов.
- **Rate-limit retry — заголовок `x-ratelimit-retry` (секунды), НЕ стандартный `Retry-After`.** Без этого backoff в `wb_api/client.py` не успевал выждать реальное окно лимита (поймано вживую на 429 от `/sales`). Уже исправлено и проверено.
- Токен `seller-info` (common-api.wildberries.ru/api/v1/seller-info) даёт `name`/`tradeMark`/`sid` — используется для авто-имени магазина при `/addshop`.
- Комиссия: `GET /api/v1/tariffs/commission` (common-api), `?locale=ru`, ответ `report[]` с `subjectName` (совпадает 1:1 с `orders.subject`) и полями `kgvpSupplier`/`kgvpMarketplace`/`kgvpPickup`/`kgvpBooking`/`kgvpSupplierExpress`/`paidStorageKgvp`. Лимит 1 запрос/мин (всплеск 2).
- Логистика: `GET /api/v1/tariffs/box` (common-api), `?date=ГГГГ-ММ-ДД` (обязателен), ответ `response.data.warehouseList[]` со СТРОКОВЫМИ числами через запятую (`"78,2"`) и `"-"` как "не применимо". `boxDelivery{Base,Liter,CoefExpr}` — для FBW, `boxDeliveryMarketplace{...}` — для FBS. **warehouseName в этом списке — крупные логистические хабы (страны/города), НЕ конкретные фулфилмент-центры из `/orders`** — прямое совпадение по имени редкость/не гарантировано.
- `/api/tariffs/v1/acceptance/coefficients` (с полями конкретных складов типа "Сц Брянск 2") — **временно отключён самим WB**, не наша проблема.
- Рейтинг/отзывы: `POST /api/analytics/v2/item-rating` (seller-analytics-api), body обязательно требует `currentPeriod`+`orderBy`+`offset`, до 50 `nmIds` за раз. **`currentPeriod.end` не может быть сегодняшней датой** (400 "current period end date cannot be today") — использовать вчера. `data.items[].feedbackRating.current` — рейтинг, НЕ зависит от периода. `data.items[].feedbackCount.current` — ЗАВИСИТ от периода (не лайфтайм-счётчик отзывов) — максимум 365 дней в одном запросе.

## Известные открытые вопросы

- `TELEGRAM_ADMIN_CHAT_ID` — опционально, для оперативных алертов/смоук-теста, НЕ для уведомлений пользователей (те идут в `shops.telegram_chat_id`).
- Хостинг — VPS или постоянно включённая машина (планировщик не работает, если процесс не запущен).
- Полный текст плана-ревизии 2 (обоснования решений) — в истории сессии Claude Code / `~/.claude/plans/parallel-mixing-whale.md`.

## Команды

```bash
pip install -r requirements.txt
cp .env.example .env   # заполнить TELEGRAM_BOT_TOKEN и TOKEN_ENCRYPTION_KEY
python scripts/check_connectivity.py
python scripts/notify_cli.py start-notifying --shop-id 1   # включить рассылку (отсечь бэкфилл)
python scripts/run_scheduler.py                            # фоновый процесс: опрос + уведомления
python scripts/sync_cli.py register --owner-user-id <id> --chat-id <id> --token-file <путь>
pytest
```
